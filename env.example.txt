# ============================================================
# Sistema de Comissões - Young Empreendimentos
# Arquivo de exemplo de variáveis de ambiente
# Renomeie para .env e preencha com seus valores reais
# ============================================================

# ==================== SUPABASE (OBRIGATÓRIO) ====================
# URL do seu projeto Supabase (encontre em Settings > API)
SUPABASE_URL=https://seu-projeto.supabase.co

# Chave de API do Supabase - USE A SERVICE_ROLE KEY (não a anon key)
# Encontre em Settings > API > service_role (secret)
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ==================== SIENGE API (OBRIGATÓRIO) ====================
# URL base da API Sienge (geralmente não precisa mudar)
SIENGE_BASE_URL=https://api.sienge.com.br/youngemp/public/api

# Credenciais da API Sienge
SIENGE_USERNAME=seu_usuario_sienge
SIENGE_PASSWORD=sua_senha_sienge

# ID da empresa no Sienge
SIENGE_COMPANY_ID=5

# ==================== E-MAIL SMTP (RECOMENDADO) ====================
# Configuração para envio de e-mails de aprovação
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Para Gmail: use uma Senha de App (não sua senha normal)
# Gere em: https://myaccount.google.com/apppasswords
SMTP_USER=seu.email@gmail.com
SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# E-mail que aparece como remetente
EMAIL_FROM=sistema@youngempreendimentos.com.br

# ==================== FLASK (OBRIGATÓRIO) ====================
# Chave secreta do Flask - GERE UMA NOVA para produção!
# Comando: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=gere_uma_chave_aleatoria_de_64_caracteres_aqui

# Porta do servidor (padrão: 5000)
FLASK_PORT=5000

# Modo debug - DEVE SER False EM PRODUÇÃO!
FLASK_DEBUG=False

# ==================== PRODUÇÃO ====================
# URL do servidor em produção (para configuração de CORS)
# Exemplo: https://comissoes.youngempreendimentos.com.br
PRODUCTION_URL=http://localhost:5000
